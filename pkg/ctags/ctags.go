// Package ctags provides access to github.com/sourcegraph/zoekt/internal/ctags
//
// This package is a mirror of the internal package with the same name, providing
// access to its exported identifiers.
package ctags

import original "github.com/sourcegraph/zoekt/internal/ctags"

// Accessor is re-exported from the internal package.
const Accessor = original.Accessor

// Chapter is re-exported from the internal package.
const Chapter = original.Chapter

// Class is re-exported from the internal package.
const Class = original.Class

// Constant is re-exported from the internal package.
const Constant = original.Constant

// Define is re-exported from the internal package.
const Define = original.Define

// Enum is re-exported from the internal package.
const Enum = original.Enum

// EnumConstant is re-exported from the internal package.
const EnumConstant = original.EnumConstant

// Field is re-exported from the internal package.
const Field = original.Field

// Function is re-exported from the internal package.
const Function = original.Function

// Interface is re-exported from the internal package.
const Interface = original.Interface

// Library is re-exported from the internal package.
const Library = original.Library

// Local is re-exported from the internal package.
const Local = original.Local

// Method is re-exported from the internal package.
const Method = original.Method

// MethodAlias is re-exported from the internal package.
const MethodAlias = original.MethodAlias

// MethodSpec is re-exported from the internal package.
const MethodSpec = original.MethodSpec

// Module is re-exported from the internal package.
const Module = original.Module

// Namespace is re-exported from the internal package.
const Namespace = original.Namespace

// NoCTags is re-exported from the internal package.
const NoCTags = original.NoCTags

// Object is re-exported from the internal package.
const Object = original.Object

// Other is re-exported from the internal package.
const Other = original.Other

// Package is re-exported from the internal package.
const Package = original.Package

// ScipCTags is re-exported from the internal package.
const ScipCTags = original.ScipCTags

// Section is re-exported from the internal package.
const Section = original.Section

// SingletonMethod is re-exported from the internal package.
const SingletonMethod = original.SingletonMethod

// Struct is re-exported from the internal package.
const Struct = original.Struct

// Subsection is re-exported from the internal package.
const Subsection = original.Subsection

// Trait is re-exported from the internal package.
const Trait = original.Trait

// Type is re-exported from the internal package.
const Type = original.Type

// TypeAlias is re-exported from the internal package.
const TypeAlias = original.TypeAlias

// Union is re-exported from the internal package.
const Union = original.Union

// UniversalCTags is re-exported from the internal package.
const UniversalCTags = original.UniversalCTags

// UnknownCTags is re-exported from the internal package.
const UnknownCTags = original.UnknownCTags

// Variable is re-exported from the internal package.
const Variable = original.Variable

// CTagsParser is re-exported from the internal package.
type CTagsParser = original.CTagsParser

// CTagsParserType is re-exported from the internal package.
type CTagsParserType = original.CTagsParserType

// Entry is re-exported from the internal package.
type Entry = original.Entry

// LanguageMap is re-exported from the internal package.
type LanguageMap = original.LanguageMap

// ParserBinMap is re-exported from the internal package.
type ParserBinMap = original.ParserBinMap

// SymbolKind is re-exported from the internal package.
type SymbolKind = original.SymbolKind

// NewCTagsParser is re-exported from the internal package.
var NewCTagsParser = original.NewCTagsParser

// NewParserBinMap is re-exported from the internal package.
var NewParserBinMap = original.NewParserBinMap

// ParseSymbolKind is re-exported from the internal package.
var ParseSymbolKind = original.ParseSymbolKind

// ParserToString is re-exported from the internal package.
var ParserToString = original.ParserToString

// StringToParser is re-exported from the internal package.
var StringToParser = original.StringToParser
